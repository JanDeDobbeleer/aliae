name: Discord
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Discord announcements are disabled in this fork."
