name: Inno
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
    - name: Disabled
      run: echo "Inno publishing and downstream notifications are disabled in this fork."

