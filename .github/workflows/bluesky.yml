name: Bluesky
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Bluesky announcements are disabled in this fork."
