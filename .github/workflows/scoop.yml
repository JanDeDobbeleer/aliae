name: Scoop
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Scoop publishing is disabled in this fork."
